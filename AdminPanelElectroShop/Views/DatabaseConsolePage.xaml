<Page x:Class="AdminPanelElectroShop.Views.DatabaseConsolePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DatabaseConsolePage">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="🗄️ SQL-консоль" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

        <TextBox x:Name="SqlTextBox" Grid.Row="1" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap" FontFamily="Consolas" Padding="10"
                 Text="SELECT * FROM users LIMIT 50;"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,10">
            <Button Content="Выполнить" Width="120" Margin="0,0,10,0"
                    Click="RunQuery_Click" Background="#1F6FEB" Foreground="White"/>
            <TextBlock x:Name="ResultInfoText" VerticalAlignment="Center" Foreground="#60708F"/>
        </StackPanel>

        <DataGrid x:Name="ResultGrid" Grid.Row="3" AutoGenerateColumns="True" IsReadOnly="True"/>
    </Grid>
</Page>
