<Page x:Class="AdminPanelElectroShop.Views.ProductsModerationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AdminPanelElectroShop.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductsModerationPage">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="✅ Модерация товаров" 
                   FontSize="24" FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <TextBlock x:Name="PendingCountText" 
                   Grid.Row="1"
                   FontSize="14"
                   Foreground="#60708F"
                   Margin="0,0,0,15"/>

        <DataGrid x:Name="ProductsGrid" Grid.Row="2" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True"
                  SelectionMode="Single" SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="250"/>
                <DataGridTextColumn Header="Бренд" Binding="{Binding Brand}" Width="120"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="120"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="80"/>
                <DataGridTemplateColumn Header="Действия" Width="250">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="✅ Одобрить" Width="80" Margin="0,0,5,0" 
                                        Click="Approve_Click" Background="#28A745" Foreground="White"/>
                                <Button Content="❌ Отклонить" Width="80" Margin="0,0,5,0" 
                                        Click="Reject_Click" Background="#DC3545" Foreground="White"/>
                                <Button Content="👁️ Детали" Width="70" 
                                        Click="ViewDetails_Click" Background="#1F6FEB" Foreground="White"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
