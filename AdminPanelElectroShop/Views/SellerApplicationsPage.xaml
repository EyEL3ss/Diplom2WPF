<Page x:Class="AdminPanelElectroShop.Views.SellerApplicationsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SellerApplicationsPage">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="📝 Заявки продавцов" FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>
        <TextBlock x:Name="PendingCountText" Grid.Row="1" Foreground="#60708F" Margin="0,0,0,10"/>

        <DataGrid x:Name="ApplicationsGrid" Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False" IsReadOnly="True"
                  SelectionMode="Single" SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Пользователь" Binding="{Binding Email}" Width="180"/>
                <DataGridTextColumn Header="Магазин" Binding="{Binding StoreName}" Width="180"/>
                <DataGridTextColumn Header="ИНН" Binding="{Binding INN}" Width="100"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="120"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="110"/>
                <DataGridTemplateColumn Header="Действия" Width="230">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="✅" Width="40" Margin="0,0,5,0"
                                        ToolTip="Одобрить"
                                        Click="Approve_Click" Background="#28A745" Foreground="White"/>
                                <Button Content="❌" Width="40" Margin="0,0,5,0"
                                        ToolTip="Отклонить"
                                        Click="Reject_Click" Background="#DC3545" Foreground="White"/>
                                <Button Content="👁️" Width="40"
                                        ToolTip="Детали"
                                        Click="Details_Click" Background="#1F6FEB" Foreground="White"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
